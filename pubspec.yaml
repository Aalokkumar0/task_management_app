name: task_management_app
description: A polished Flutter Task Management App — Flodo AI Assignment
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.2
  path: ^1.9.0
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  uuid: ^4.4.0
  intl: ^0.19.0
  flutter_slidable: ^3.1.0
  reorderables: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true